fn main() {
    // ! represent a rust macro
    println!("hello world");
}

// generate a binary executable that can be executed on the operating system where we ran this code
// rustc init.rs
// to run the executable .\init.exe [windows] | ./init [linux/macos] => hello world
